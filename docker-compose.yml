services:
  api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      NODE_ENV: development
      PORT: 3000
      USER_AGENT: wc-technicaltest-backend

volumes:
  node_modules:
